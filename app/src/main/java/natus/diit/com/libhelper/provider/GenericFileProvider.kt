package natus.diit.com.libhelper.provider

import android.support.v4.content.FileProvider

/**
 * Class for providing access to internal dir (for android.version >= 7.0)
 */
class GenericFileProvider : FileProvider()